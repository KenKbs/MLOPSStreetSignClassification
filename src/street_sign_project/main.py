# Idea: Here we do orchestration later if frontend is up
# This then should start:
# the API itself
# the frontend, which uses the API
# so the user actually can "interact" with our application
# so this file should stay lightweight orchestrator only, no buissness logic
